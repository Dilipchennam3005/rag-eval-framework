data:
  raw_dir: data/raw
  sparse_encoder_path: data/sparse_encoder.pkl
  registry_db: data/registry.db

pinecone:
  index_name: rag-eval-hybrid
  dimension: 1536

companies:
  - ticker: AAPL
    cik: "0000320193"
  - ticker: MSFT
    cik: "0000789019"
  - ticker: JPM
    cik: "0000019617"

ingestion:
  filing_type: 10-K
  num_filings: 2

chunking:
  fixed_size:
    chunk_size: 2000
    overlap: 200
  document_aware:
    max_chunk_size: 3000
    min_chunk_size: 200

embedding:
  model: text-embedding-3-small
  batch_size: 100

generation:
  model: gpt-4o-mini
  prompt_version: v1

retrieval:
  top_k: 5
  reranker_model: cross-encoder/ms-marco-MiniLM-L-6-v2
  hybrid:
    sparse_weight: 0.3

observability:
  mlflow_experiment: rag-eval-framework
  mlflow_tracking_uri: sqlite:///mlflow.db
  wandb_project: rag-eval-framework
